header.header
    .container-fluid
        .row.align-items-center
            .col-3 
                .inner-logo
                        a(href="/admin/dashboard") Admin    
            .col9
                div(class ="text-right")
                    a(href=`/${prefixAdmin}/my-account`, class="btn btn-primary btn-sm mr-2") #{user.fullName}
                    a.btn.btn-danger.btn-sm(
                        href=`/admin/auth/logout`
                    ) Đăng xuất